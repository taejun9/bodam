{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the BODAM main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
